{
  "name": "plex-lib",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "vite --host 127.0.0.1",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "dump:metadata": "node scripts/dump-library-metadata.js",
    "dump:metadata:short": "PLEX_DUMP_SHORT=1 node scripts/dump-library-metadata.js",
    "check": "node --check scripts/dump-library-metadata.js && node --check src/plex.js && node --check src/browserPlex.js && node --check src/storage.js && node --check src/main.js"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "idb-keyval": "^6.2.4"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  }
}
